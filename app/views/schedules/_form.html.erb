<%= form_with(model: schedule, local: true) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
        <% schedule.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :documento_paciente %>
    <%= form.text_field :documento_paciente %>
  </div>

  <div class="field">
    <%= form.label :documento_medico %>
    <%= form.text_field :documento_medico %>
  </div>

  <div class="field">
    <%= form.label :dia %>
    <%= form.date_select :dia %>
  </div>

  <div class="field">
    <%= form.label :hora %>
    <%= form.time_select :hora %>
  </div>

  <div class="field">
    <%= form.label :cita_id %>
    <%= form.text_field :cita_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
