<%= form_with(model: cita, local: true) do |form| %>
  <% if cita.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cita.errors.count, "error") %> prohibited this cita from being saved:</h2>

      <ul>
        <% cita.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre_paciente %>
    <%= form.text_field :nombre_paciente %>
  </div>

  <div class="field">
    <%= form.label :documento_paciente %>
    <%= form.text_field :documento_paciente %>
  </div>

  <div class="field">
    <%= form.label :telefono_paciente %>
    <%= form.text_field :telefono_paciente %>
  </div>

  <div class="field">
    <%= form.label :nombre_medico %>
    <%= form.text_field :nombre_medico %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :hora %>
    <%= form.time_select :hora %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
